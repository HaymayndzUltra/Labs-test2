# Deployment Narrative

- **Project**: ${project_name}
- **Version**: ${version}
- **Environments**: ${environment_count}
- **Primary Environment**: ${primary_environment}

Outline the promotion strategy, validation checkpoints, and rollback approach. Attach the
machine-readable deployment_plan.json alongside this narrative for auditing.
